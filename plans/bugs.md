- [x] fixed: ranking now prices the detour (2 ct/kWh per km, unit-tested) — mismatch between the filters and proposals when clicking on charge now. this should be tested more thoroughly with unit tests i think, currently the distance is a little bit glitched. a 1.4km charging station matching my filters was not shown in my suggestions when the maximum distance was set too high, then other stations were in the preferred top.
- [x] fixed: spinner sits on a white card (same pattern as the zoom hint) — loading spinner while planning the route should have a standard background, otherwise it's not visible
- [x] fixed: one StationCard for both views, operator headline, ic_send inside the card, trip review adds the Ankunft/laden line — design philosophy: the charge now charging cards and the cards in the plan-route-review view are very different currently, but they should be similar components. i prefer the sleek look of the review route view, the button icon also makes more sense there. but the information order in the charge now menu makes more sense, headline always network provider, address and kw below and when we're in the route review view, we just get an additional line with how long we should charge and when we'll arrive.
- [x] fixed: bar icons pinned dark via enableEdgeToEdge SystemBarStyle — status bar should be always black now, otherwise you can't see them above the map
- [x] fixed: operator list seeded from the site store (instant after restart) + loading spinner while empty — network providers should be saved in a db or other so the window takes only one time to load long, and then afterwards it's cached. also, a loading spinner when clicking on networks would be nice. currently the sidebar just closes and then the map is visible without any further user feedback
- [ ] the garage's consumption slider shows a stale value after the number was changed elsewhere. `SelectedVehiclePanel` keys its slider state on `vehicle.displayName` (GarageScreen.kt:159), so editing the consumption on the advanced screen and coming back leaves the garage showing the old kWh/100 km until the screen is rebuilt — the stored profile is correct, only the label lies. Keying on the consumption itself would fight the drag; the value probably belongs in the ViewModel like the vehicle form's fields.
- [x] fixed: NavDisplay's default push/pop was a 700 ms fade (nav3's `DEFAULT_TRANSITION_DURATION_MILLISECOND`), run on every back-button pop — overrode `transitionSpec`/`popTransitionSpec` to a 200 ms fade in MainActivity.kt, leaving the predictive-back *gesture*'s own fast spring untouched (which is why the swipe felt fine and only the button crawled). — the full-screen menus opened from the drawer (Networks, Car data, Subscriptions) took a long, visible beat to close when you pressed back; all three stalled the same way because it was the shared NavDisplay pop, not any one screen's content.
- [ ] leaving the Networks screen with the back button — without tapping "Filter übernehmen" — keeps the staged ticks instead of the committed filter, so the checkboxes (and the still-enabled Confirm button) lie about what's actually active. `NetworksViewModel` seeds `pending` from the committed `preferredOperators` once in `init` (NetworksViewModel.kt:61) and only changes it in `onNetworkToggled`; the view model outlives the screen (`koinViewModel`, SharedUiModule.kt:23), so that one-time seed never re-runs and the stale `pending` is still there on reopen. Closing without Confirm should discard the staged selection — reset `pending` back to `committed` on leave (a `reset()`/`onClose` the screen fires from its `onDispose` or back handler), so the ticks always reflect the filter that's really in force.
