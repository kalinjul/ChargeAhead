# fastlane is pinned by major version only: it talks to two moving APIs
# (Play Developer API, App Store Connect) and a stale pin there breaks
# uploads more often than a minor update does.
source "https://rubygems.org"

gem "fastlane", "~> 2.228"
